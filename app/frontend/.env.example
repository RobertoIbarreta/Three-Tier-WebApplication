# Production build: set to your public API URL (ALB HTTPS or custom api.example.com).
# Include scheme and no trailing slash, e.g. https://api.example.com
# Leave empty for local dev (Vite proxy to backend on :8080).
VITE_API_URL=
